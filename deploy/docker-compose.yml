services:
  api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8080:8080"
    environment:
      PGDCT_DATABASE_URL: sqlite+aiosqlite:///./data/pgdct.db
      PGDCT_LOG_LEVEL: INFO
    volumes:
      - pgdct-data:/app/data

volumes:
  pgdct-data:
